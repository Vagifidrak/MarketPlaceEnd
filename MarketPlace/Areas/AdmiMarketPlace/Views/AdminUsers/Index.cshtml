@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/AdmiMarketPlace/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Istifadeciler</h2>

<table class="table table-hover table-striped table-bordered">
    <thead class="text-center">
        <tr>
            <th>Username</th>
            <th>Email</th>
            <th>Role</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody class="text-center">
        @*@foreach (var user in Model)
        {
            <tr>
                <td>@user.UserName</td>
                <td>@user.Email</td>
                <td>
                    @{
                        var roles = await userManager.GetRolesAsync(user);
                        string userRole = "";
                        foreach (var role in roles)
                        {
                            userRole += role + " ";
                        }
                    }
                </td>
                <td>
                    <a href="@Url.Action("Edit", "AdminUsers", new { id = user.Id })" class="btn btn-block btn-outline-warning">
                        <i class="fas fa-pencil-alt"></i>
                    </a>
                    <a href="@Url.Action("AddRole", "AdminUsers", new { id = user.Id })" class="btn btn-block btn-outline-success">
                        <i class="fas fa-user-plus"></i>
                    </a>
                </td>
                </tr>}*@

                </tbody>
                </table>